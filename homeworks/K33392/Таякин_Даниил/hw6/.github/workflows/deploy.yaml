name: Deploy to the server

on:
  push:
    branches:
      - lab-4

jobs:
  deploy_dashboard:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Update code
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        script: cd /root/ITMO-ICT-Backend-2024 && git checkout lab-4 && git pull && cd labs/K33392/Таякин_Даниил && docker compose up --build -d
    